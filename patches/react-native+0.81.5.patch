diff --git a/node_modules/react-native/React/Base/RCTConvert.h b/node_modules/react-native/React/Base/RCTConvert.h
index 88e5963..bca5623 100644
--- a/node_modules/react-native/React/Base/RCTConvert.h
+++ b/node_modules/react-native/React/Base/RCTConvert.h
@@ -241,7 +241,12 @@ RCT_EXTERN NSArray *RCTConvertArrayValue(SEL, id);
     static NSDictionary *mapping;                                                           \
     static dispatch_once_t onceToken;                                                       \
     dispatch_once(&onceToken, ^{                                                            \
-      mapping = values;                                                                     \
+      @try {                                                                                \
+        mapping = values;                                                                   \
+      } @catch (NSException *e) {                                                           \
+        NSLog(@"[RCT_ENUM_CONVERTER] Failed to create mapping for %s: %@", #type, e);       \
+        mapping = @{};                                                                      \
+      }                                                                                     \
     });                                                                                     \
     return _RCT_CAST(type, [RCTConvertEnumValue(#type, mapping, @(default), json) getter]); \
   }
@@ -256,7 +261,12 @@ RCT_EXTERN NSArray *RCTConvertArrayValue(SEL, id);
     static NSDictionary *mapping;                                                                \
     static dispatch_once_t onceToken;                                                            \
     dispatch_once(&onceToken, ^{                                                                 \
-      mapping = values;                                                                          \
+      @try {                                                                                     \
+        mapping = values;                                                                        \
+      } @catch (NSException *e) {                                                                \
+        NSLog(@"[RCT_MULTI_ENUM_CONVERTER] Failed to create mapping for %s: %@", #type, e);      \
+        mapping = @{};                                                                           \
+      }                                                                                          \
     });                                                                                          \
     return _RCT_CAST(type, [RCTConvertMultiEnumValue(#type, mapping, @(default), json) getter]); \
   }
