diff --git a/node_modules/@react-native-community/javascriptcore/React-jsc.podspec b/node_modules/@react-native-community/javascriptcore/React-jsc.podspec
index 9654b29..08c7d61 100644
--- a/node_modules/@react-native-community/javascriptcore/React-jsc.podspec
+++ b/node_modules/@react-native-community/javascriptcore/React-jsc.podspec
@@ -23,7 +23,11 @@ Pod::Spec.new do |s|
   s.author                 = "Meta Platforms, Inc. and its affiliates"
   s.platforms              = { :ios => "15.1", :tvos => "15.1", :visionos => "1.0", :osx => "10.15" }
   s.source                 = { :git => "https://github.com/react-native-community/javascriptcore.git", :tag => "#{s.version}" }
-  s.source_files           = "common/*.{cpp,h}", "ios/*.{mm,h}"
+  # iOS26_JSC_FIX: Only include common/ (JSCRuntime core). The ios/ directory
+  # contains Bridgeless runtime files (RCTJscInstance/Factory) that require
+  # react/runtime/JSRuntimeFactory.h and create circular pod dependencies.
+  # For old architecture (Bridge), only the JSCRuntime jsi bridge is needed.
+  s.source_files           = "common/*.{cpp,h}"
   s.compiler_flags = folly_compiler_flags + ' ' + boost_compiler_flags
   s.weak_framework         = "JavaScriptCore"
   s.pod_target_xcconfig    = { "DEFINES_MODULE" => "YES" }
@@ -31,8 +35,5 @@ Pod::Spec.new do |s|
 
   s.dependency "RCT-Folly", folly_version
   s.dependency "DoubleConversion"
-  s.dependency "React-jsitooling"
-  s.dependency "React-cxxreact"
   s.dependency "React-jsi"
-  s.dependency "React-jsiexecutor"
 end
